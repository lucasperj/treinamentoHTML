// Arvore DOM - Document Object Model - Interface de objetos que permite a manipulacao de objetos na pagina web.

// o objeto global/geral/mae/pai de todos os elementos no navegador e o window

// depois window.document 

//'dentro' de #document --> consta as tags html --> head e body sao childs de HTML e assim por diante...

/* o DOM permite manipulacao de alguns elementos no proprio console, 
por exemplo, pesquisou container no console, depois criou uma const
const h1 = container.querySelector('h1'); para achar o h1 no html, depois digitou h1 no console. 
depois manipulou o h1, como exemplo, h1.style.backgroundColor = 'red';
utilizamos o js para manipular os elementos da pagina, gracas ao DOM.
*/
